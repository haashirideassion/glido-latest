# Local dev — leave empty, Vite proxy handles /api → localhost:3001
VITE_API_BASE_URL=

# Production — defaults to the Cloud Run backend below if left unset in a
# production build (see frontend/src/lib/apiBase.ts). Override here only if
# the backend URL changes.
# VITE_API_BASE_URL=https://glido-backend-prod-175164625810.asia-south1.run.app
